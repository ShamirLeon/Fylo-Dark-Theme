const NavLink = ({ title }) => {
  return (
    <a
      href="#"
      className="font-raleway text-white text-[14px] hover:font-extrabold hover:underline"
    >
      {title}
    </a>
  );
};

export default NavLink;
